import { ReactNode } from "react";
interface contentItemProps {
    id: string;
    children?: ReactNode;
    marginBottom?: number;
    paddingTop?: number;
    label: string;
}
declare const _default: (props: contentItemProps) => JSX.Element;
export default _default;
